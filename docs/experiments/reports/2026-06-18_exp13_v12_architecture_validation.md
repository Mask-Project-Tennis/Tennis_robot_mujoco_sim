# Exp13 — V12 架构对比验证（V11 vs V12 可靠性）

**日期**: 2026-06-18
**实验编号**: exp13_v12_architecture_validation
**数据目录**: `experiment_data/v11_v12_comparison/`
**CSV**: `results.csv` (403 行, seeds 1-100)
**运行时间**: ~8 min, 0 failed

## 目的

<!-- 人工填写 -->

验证 V12 新管线架构（EpisodeRunner + MPCController 策略化）相对于 V11 旧架构（inline main() 循环）的可靠性和性能差异。对比力矩模式（默认）和位置模式（`--position-mode`）下的命中率、位置误差和求解时间。

## 参数

### 通用参数

| 参数 | 值 |
|------|-----|
| 脚本 | V11 (`rm65_mpc_v11.py`) vs V12 (`rm65_mpc_v12.py`) |
| 发球模式 | serve_box |
| 球速 | 7 m/s |
| dt | 0.005s |
| replan_interval | 20 |
| 命中阈值 | pos_error < 0.153m（球半径 0.033 + 球拍半径 0.12） |

### 实验矩阵

| 配置 | 版本 | 模式 | Seeds | 说明 |
|------|------|------|-------|------|
| V11_torque | V11 | 力矩（默认） | 1-100 | 旧架构基准 |
| V12_torque | V12 | 力矩（默认） | 1-100 | 新架构对比 |
| V11_position | V11 | 位置（`--position-mode`） | 1-100 | 双模式基准 |
| V12_position | V12 | 位置（`--position-mode`） | 1-100 | 双模式对比 |

总运行数: 100 seeds × 4 配置 = 400（实际有效 403，seed 85 含重复行）

## 结果

### 力矩模式（核心对比）

| 指标 | V11 (n=98) | V12 (n=98) | 变化 |
|------|-----|-----|------|
| **命中率** | 42.9% (42/98) | **85.7%** (84/98) | **+42.8pp** |
| **命中误差** | 0.076 ± 0.032 m | 0.103 ± 0.026 m | V11 更准（选择偏差） |
| **未中误差** | 0.379 ± 0.134 m | **0.413 ± 0.035 m** | V12 一致性高 4 倍 |
| **全部误差** | 0.250 ± 0.182 m | **0.147** ± 0.112 m | -41% |
| **vel_error** | 1.895 ± 0.104 m/s | **1.977** ± 0.049 m/s | +4.3% (更准+更稳) |
| **首次规划** | 860 ± 17 ms | **800** ± 10 ms | -7% |
| **总耗时** | 1.44 ± 0.06 s | **1.25** ± 0.03 s | -13% |
| **主动击球** | 39 | **82** | 2.1 倍 |

### 位置模式（辅助对比）

| 指标 | V11 (n=98) | V12 (n=97) | 变化 |
|------|-----|-----|------|
| **命中率** | 12.2% (12/98) | **23.7%** (23/97) | +11.5pp |
| **命中误差** | 0.129 ± 0.019 m | **0.114** ± 0.018 m | -12% |
| **未中误差** | **0.328** ± 0.048 m | 0.498 ± 0.091 m | +52% (更差) |
| **全部误差** | **0.304** ± 0.080 m | 0.407 ± 0.182 m | +34% (更差) |
| **首次规划** | 193 ± 5 ms | 209 ± 7 ms | +8% |
| **总耗时** | 0.76 ± 0.05 s | **0.69** ± 0.03 s | -9% |
| **主动击球** | 12 | **23** | 1.9 倍 |

## 关键数字

| 指标 | 值 |
|------|-----|
| 力矩模式命中率提升 | **+42.8pp**（42.9% → 85.7%） |
| 力矩模式误差降低 | **-41%**（0.250m → 0.147m） |
| 力矩模式规划加速 | **-7%**（860ms → 800ms） |
| 力矩模式主动击球 | V12 82 个 vs V11 39 个（2.1 倍） |
| 位置模式命中率提升 | **+11.5pp**（12.2% → 23.7%） |
| 位置模式 V12 命中误差 | **-12%**（0.114m vs 0.129m） |
| 位置模式 V12 未中误差 | **+52%**（0.498m vs 0.328m，更激进） |
| 有效样本数 | 403 runs（100 seeds × 4 配置） |

## 深度分析

### 命中重叠矩阵（力矩模式, n=97）

| | V12 命中 | V12 未中 |
|------|---------|---------|
| **V11 命中** | 36 | 6 |
| **V11 未中** | **47** | 8 |

V12 将 V11 的 **47 个未中种子转为命中**，V11 仅独中 6 个。V12 的命中率优势几乎全部来自"拯救"V11 的失败场景。

### 误差分布（决定性证据）

```
pos_error 区间    V11   V12    解读
[0.00-0.05)m      9     3     V11 有更多"完美"命中（选择偏差）
[0.05-0.10)m     24    34     V12 主峰左翼
[0.10-0.15)m      7    45     ★ V12 主峰值（45 个命中聚集于此）
─────────────────────────────────────────── 0.153m 命中阈值
[0.15-0.20)m     10     1     V12 几乎不在此区间（干净截断）
[0.20-0.30)m     11     0     ★ V12 零个！
[0.30-0.50)m     25    14     V12 第二峰（失败模式）
[0.50-10.0)m     11     0     ★ V12 无灾难性失败！
```

**V12 误差分布是双峰的**：81% 的结果聚集在 <0.15m（命中区），14% 在 0.30-0.50m（一致失败），中间区间（0.15-0.30m）和灾难区（>0.50m）**完全为空**。

**V11 误差分布是连续的**：从 0.02m 到 2.0m 均匀散布，无截断效应。

### 三层根因分析

| 层 | 现象 | 根因 | 证据 |
|----|------|------|------|
| **1. 代价函数状态泄漏** | V11 46 个未中连续分布在 0.15-10m 全区间 | V11 复用 `base_cost_fn`（跨 ~10 次重规划累积 `update_target`/`update_weights`），tube 映射和权重残留未完全清零 → 规划逐渐偏离最优 | V12 干净代价在 [0.15-0.30)m 区间**零个**结果 |
| **2. 主动击球速度** | V12 主动击球 81 个 vs V11 39 个（2.1 倍） | V11 stale cost 倾向保守控制量（接近当前关节角），球拍速度不足 → 被动接触而非主动击打 | V12 主动/总命中比 81/84 = 96%；V11 39/42 = 93% |
| **3. 失败模式受控** | V12 全部 14 个未中在 0.30-0.50m 窄带（std=0.035m） | V12 干净代价在某些场景过于激进，但偏差始终有界（<0.50m）；V11 stale cost 可产生灾难性偏差（11 个 >0.50m） | V11 有 11 个 >0.50m（灾难性），V12 为 0 |

### 规划耗时与命中无关

| 版本 | 命中时首次规划 | 未中时首次规划 |
|------|-------------|-------------|
| V11 | 857ms | 863ms |
| V12 | 800ms | 797ms |

规划耗时由 iLQR 迭代数（30 次）决定，与命中结果**零相关**。V12 快 60ms 纯粹因为统一走 `do_replan` 无分支开销，不是因为"规划更少"。

### 位置模式两极化分析

V12 位置模式呈现明显的两极化：命中时误差 0.114m（优于 V11 的 0.129m），但未中时误差 0.498m（远差于 V11 的 0.328m）。

| V12 位置模式 | 命中段（23 个） | 未中段（74 个） |
|-------------|---------------|---------------|
| pos_error | 0.114 ± 0.018 m | 0.498 ± 0.091 m |
| 行为 | 干净代价及时收敛 | 干净代价过于激进（大角度转向） |

V11 位置模式的 stale cost 保留了"保守性倾向"——倾向于较小控制量，所以未中时偏差保持 0.328m（std=0.048m，一致性高）。V12 无此保守性，在困难场景下产生更激进的控制，导致偏差更大。

两者的低命中率（12-24%）根本原因是**位置模式 PD 参数未针对 7 m/s 场景调优**，非架构问题。

## 结论

### 数据观察（Agent 生成）

1. 力矩模式下 V12 命中率 85.7% vs V11 42.9%，提升 42.8 个百分点 @ 7m/s，样本量 n=98/组
2. V12 力矩模式主动击球 82 个 vs V11 39 个（2.1 倍），说明 V12 球拍在击球时刻有更高速度
3. V12 力矩模式未中误差标准差 0.035m（V11 0.134m），一致性高 4 倍，说明未中场景的失败模式高度统一
4. V12 力矩模式首次规划 800ms（V11 860ms），减少 60ms（-7%），总耗时减少 13%（1.25s vs 1.44s）
5. 位置模式下两者命中率均低于 25%（V11 12.2%, V12 23.7%），显著低于力矩模式，位置模式需独立 PD 调优
6. V12 位置模式命中误差 0.114m（V11 0.129m），命中时精度优于 V11，但未中误差 0.498m（V11 0.328m），V12 在位置模式下表现更两极化

### 分析与决策

#### 1. Stale cost 精确机制（根因定位）

V11 命中率 42.9% 的根因是**代价函数状态泄漏**（cost function state leakage），不是控制算法本身的问题：

- **旧 V11 行为**：创建一个 `HittingCost`（或 `TubeHittingCostWrapper`）对象后，跨 ~10 次重规划反复调用 `update_target(p_hit)` / `update_weights(Q_p)` 增量更新目标点和权重
- **Bug 本质**：`update_target` / `update_weights` 只更新了终端目标点和权重矩阵，**未完全重置** `R_schedule`（控制代价退火表）、tube 候选窗口映射、内部雅可比缓存等隐式状态
- **累积效应**：每次重规划残留 ~10% 旧状态，10 次重规划后代价函数已偏离原始形式 → Riccati 增益 K_k 指向错误方向 → 规划轨迹逐渐偏移
- **失败模式**：V11 的 46 个未中连续分布在 0.15-10m 全区间，其中 11 个 >0.50m（灾难性偏差），而 V12 全部未中在 0.30-0.50m 窄带（std=0.035m）

**控制理论判定**：MPC 要求每次重规划的代价函数精确反映当前控制目标（Mayne, 2014）。复用代价对象违反这一假设，等价于优化一个与实际目标不同的代价函数（cost function mismatch），导致递归可行性条件不成立。

**软件设计判定**：这是实现缺陷（update 不完全重置），非设计选择。V12 的修复方案是每次 replan 构造全新的 `HittingCost` 对象（`replan_core.py:266`），零状态残留，构造开销 <0.1ms 可忽略。

#### 2. 与 exp14 PD 参数差异的关联

exp10（旧 V11, Kp=20 最优 100%）与 exp14（V12, Kp=500 最优 80%）的 25× Kp 差异根因是同一个 stale cost bug：

- 位置模式下 B 矩阵 = `dt·M⁻¹·diag(Kp)`，Kp 直接决定 iLQR 梯度信号强度
- Kp=20 时梯度信号弱（B ≈ 0.1·M⁻¹），但旧 V11 的 stale cost 残留（R_schedule 保守性 + tube 映射惯性）补偿了弱梯度，提供隐式动量使优化器仍能收敛
- V12 去除 stale cost 后，Kp=20 的弱梯度无法驱动优化器收敛，需要 Kp=500（B ≈ 2.5·M⁻¹，梯度强 25×）才能获得足够的控制权威

**验证预测**：若用 V12 跑 Kp=[10,15,20,25,30,35,40,50]（exp10 的范围），预期命中率显著低于 exp10 的 81-100%。

#### 3. 下一步行动

- ✅ exp14 已完成 V12 位置模式 PD 调优（Kp=500 Kr=0.15 → 80.0%），缩小了与力矩模式（85.7%）的差距
- 旧 V11 的 stale cost 行为已通过 exp13 数据归档，不再需要复现
- 未来如需提升性能，可安全引入的复用机制（需保证完全重置 + 测试验证）：跨 replan 线性化种子（~8ms/replan）、击球窗口位移复用（~5ms/replan）
